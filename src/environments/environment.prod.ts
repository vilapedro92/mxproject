export const environment = {
  production: true,
  baseUrl: "http://jsonplaceholder.typicode.com/",
  endpoints: {
    users: "users",
    user: "user",
    albums: "albums",
    posts: "posts",
    comments: "comments",
  }
};
